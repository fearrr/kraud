<% breadcrumb :edit_comment %>
<%= render 'shared/comments_error_messages' %>
<%= form_for @comment do |f| %>
    <%= f.label :name, 'Имя' %>
    <%= f.text_field :name, 'required' => '', class: 'form-control' %>
    <%= f.label :email, 'Email' %>
    <%= f.text_field :email, 'type' => 'email', 'required' => '', class: 'form-control' %>
    <%= f.label :body, 'Текст отзыва' %>
    <%= f.text_area :body, :class => 'form-control' %>
    <p>
     <div class="checkbox">
       <label>
         <%= f.check_box :public %>
         Опубликован
       </label>
     </div>
     </p>
      <% if logged_in? %>
               <ul class="nav nav-pills">
                 <li><i class="fa fa-calendar form-control"></i></li>
                 <li><%= f.text_field :created_at, "data-parsley-trigger" => "change", "data-parsley-date" => '', 'required' => '', :placeholder => 'Дата публикации', class: "datepicker form-control" %></li>
               </ul>
           <% end %>

    <%= f.submit "Сохранить", class: "btn btn-primary" %>
<% end %>