<% breadcrumb :add_caption %>
<%= render 'shared/captions_error_messages' %>
<%= form_for @caption do |f| %>
    <%= f.label :body, 'Случайный текст (50 символов)' %>
    <%= f.text_area :body, :class => 'form-control' %>
    <%= f.submit "Добавить", class: "btn btn-primary" %>
<% end %>