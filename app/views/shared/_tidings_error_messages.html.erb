<% if @tiding.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      Форма содержит <%= Russian::pluralize(@tiding.errors.count, "ошибку", "ошибки", "ошибок") %>.
    </div>
    <ul>
    <% @tiding.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>